language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
 
before_install:
- composer self-update

before_script:
- composer install --no-interaction --prefer-source

script: 
- phpunit --coverage-text
